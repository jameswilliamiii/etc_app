<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="keywords" content="coworking, entrepreneurs, tech, technology, elgin, chicago, suburbs" />
    <meta name="description" content="<%= content_for?(:desc) ? yield(:desc) : 'Coworking space for the suburban tech community' %>" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) : "Elgin Technology Center | Connect * Create * Collaborate" %>" />
    <meta property="og:description" content="<%= content_for?(:desc) ? yield(:desc) : 'Coworking space for the suburban tech community' %>" />
    <meta property="og:image" content="<%= image_path 'og_image.jpg' %>" />
    <%= favicon_link_tag 'favicon.png', :rel => 'shortcut icon' %>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Oswald' rel='stylesheet' type='text/css'>

    <title><%= content_for?(:title) ? yield(:title) : "Elgin Technology Center | Connect * Create * Collaborate" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

    <%= javascript_include_tag "application" %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','http://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-45435202-2', 'elgintech.org');
      ga('send', 'pageview');

    </script>
    <%= yield :head %>

  </head>

  <body>
    <%= render 'layouts/navigation' %>
    <%= render 'layouts/alerts_and_notices' %>

    <%= yield %>

    <%= render 'layouts/footer' %>

  </body>
</html>